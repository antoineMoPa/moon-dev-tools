/target

# What a failing UI snapshot leaves behind. The baselines themselves are committed.
/tests/snapshots/*.new.png
/tests/snapshots/*.old.png
/tests/snapshots/*.diff.png

.moontasks/

# Third-party files the browser build is made from are fetched at build time into target/ -
# see web/build/npm_packages.rs - never kept here. Their licenses are, in web/licenses/.
/web/assets/
